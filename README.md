# QuartzBot - Telegram бот для обучения и поиска вакансий

Telegram бот на Spring Boot для автоматической отправки вопросов по Java, английских слов и вакансий с hh.ru в группы и личные чаты.

## 🎯 Функциональность

### Основные возможности:

1. **Вопросы по Java** 📚
   - Ежедневная отправка случайных вопросов по Java в 7:00 утра
   - Поддержка различных топиков: Core, Spring, Hibernate, Maven, Git, REST, RDB
   - Возможность выбора конкретного топика для каждого чата
   - Сообщения автоматически закрепляются

2. **Английские слова** 🌍
   - Ежедневная отправка случайных английских слов с транскрипцией и переводом в 7:00 утра
   - Сообщения автоматически закрепляются

3. **Вакансии с hh.ru** 💼
   - Автоматический поиск и отправка вакансий по Java каждые 2 минуты
   - Фильтрация вакансий (исключает JavaScript)
   - Настраиваемые регионы поиска (по умолчанию: Россия, Беларусь)
   - Поддержка множественных регионов для каждого чата

## 🛠 Технологический стек

- **Java 21**
- **Spring Boot 3.3.3**
- **PostgreSQL** - база данных
- **Liquibase** - миграции БД
- **Telegram Bot API 6.9.7.1**
- **Gradle** - система сборки
- **Lombok** - уменьшение boilerplate кода

## 📋 Требования

- Java 21 или выше
- PostgreSQL 13+
- Gradle 7+ (или используйте `gradlew`)
- Telegram Bot Token (получить у [@BotFather](https://t.me/BotFather))

## 🚀 Запуск проекта

### 1. Клонирование репозитория

```bash
git clone <repository-url>
cd quartzbot
```

### 2. Настройка конфигурации

Скопируйте файл с примером конфигурации:

```bash
cp src/main/resources/application-local.properties.example src/main/resources/application-local.properties
```

Отредактируйте `application-local.properties` и укажите:

```properties
# Telegram Bot Configuration
bot.name=your_bot_name
bot.token=your_bot_token_from_BotFather
bot.log.chat.id=your_chat_id_for_logs

# Database Configuration
spring.datasource.url=jdbc:postgresql://localhost:5432/postgres
spring.datasource.username=postgres
spring.datasource.password=postgres
```

**Важно:** Файл `application-local.properties` уже добавлен в `.gitignore` и не будет закоммичен в Git.

### 3. Запуск приложения

```bash
./gradlew bootRun
```

Или запустите собранный JAR:

```bash
java -jar build/libs/quartzbot-0.0.1-SNAPSHOT.jar
```

## 🐳 Запуск через Docker

### Использование Docker Compose

1. Настройте переменные окружения в `docker-compose.yaml`:

```yaml
services:
  app:
    environment:
      BOT_NAME: your_bot_name
      BOT_TOKEN: your_bot_token
      BOT_LOG_CHAT_ID: your_chat_id
      SPRING_DATASOURCE_PASSWORD: your_password
```

2. Запустите контейнеры:

```bash
docker compose up -d
```

3. Проверьте логи:

```bash
docker compose logs -f app
```

## 📱 Использование бота

### Команды для пользователей

1. **Упоминание бота** `@your_bot_name`
   - Показывает меню с кнопками для получения случайного вопроса по Java или английского слова

2. **Настройки чата** `@your_bot_name_chat_settings`
   - Меню управления настройками:
     - Включение/выключение английских слов
     - Включение/выключение вопросов по Java
     - Включение/выключение вакансий
     - Управление регионами для поиска вакансий

3. **Команды для управления топиками Java:**
   - `java_topic_set_bot <topic>` - установить топик (например: `java_topic_set_bot spring`)
   - `java_topic_get_all` - показать все доступные топики

4. **Команды для управления регионами вакансий:**
   - `vacancies_area_add <название>` - добавить регион (например: `vacancies_area_add Москва`)
   - `vacancies_area_remove <название>` - удалить регион
   - `vacancies_area_list` - показать список регионов

### Расписание отправки

- **7:00 утра** - вопросы по Java и английские слова (если включены)
- **Каждые 2 минуты** - проверка и отправка новых вакансий (если включены)

## 🏗 Архитектура проекта

```
src/main/java/org/khodyko/quartzbot/
├── bots/quartz/          # Основные классы бота
│   ├── QuartzMessageBot.java      # Главный класс бота
│   ├── ScheduledExecutor.java      # Запланированные задачи
│   └── BotInitializer.java         # Инициализация бота
├── config/               # Конфигурационные классы
├── dto/hh/               # DTO для работы с API hh.ru
├── enums/                # Перечисления
├── exception/            # Исключения
├── model/                # JPA сущности
├── repository/           # JPA репозитории
└── service/              # Бизнес-логика
    └── impl/             # Реализации сервисов
```

## 🔧 Настройка

### Переменные окружения

Все настройки можно переопределить через переменные окружения:

- `BOT_NAME` - имя бота
- `BOT_TOKEN` - токен бота
- `BOT_LOG_CHAT_ID` - ID чата для логов
- `SPRING_DATASOURCE_URL` - URL базы данных
- `SPRING_DATASOURCE_USERNAME` - имя пользователя БД
- `SPRING_DATASOURCE_PASSWORD` - пароль БД
- `SPRING_PROFILES_ACTIVE` - активный профиль (по умолчанию: `local`) - позволяет использовать локальные переменные из файла `application-local.properties` при локальном запуске

### Настройка HH.ru API

В `application.properties` можно настроить:

```properties
hh.api.base-url=https://api.hh.ru
hh.vacancies.search-text=Java
hh.vacancies.professional-role-id=96
hh.vacancies.default-areas=113,16
```

Где:
- `113` - ID России
- `16` - ID Беларуси

## 📦 Деплой на удаленный сервер

### Вариант 1: Через JAR файл

1. Соберите проект:
```bash
./gradlew clean build -x test
```

2. Скопируйте JAR на сервер:
```bash
scp build/libs/quartzbot-0.0.1-SNAPSHOT.jar user@server:/opt/quartzbot/
```

3. На сервере создайте `application-local.properties` и запустите:
```bash
java -jar quartzbot-0.0.1-SNAPSHOT.jar --spring.profiles.active=local
```

### Вариант 2: Через Docker

1. Соберите образ:
```bash
docker build -t quartzbot:latest .
```

2. Сохраните образ:
```bash
docker save quartzbot:latest | gzip > quartzbot-image.tar.gz
```

3. Скопируйте на сервер и загрузите:
```bash
scp quartzbot-image.tar.gz user@server:/opt/quartzbot/
ssh user@server "cd /opt/quartzbot && gunzip -c quartzbot-image.tar.gz | docker load"
```

4. Запустите через docker-compose (см. раздел Docker выше)

## 🧪 Разработка

### Запуск тестов

```bash
./gradlew test
```

### Просмотр логов

Логи приложения выводятся в консоль. Для продакшена рекомендуется настроить логирование в файл через `logback-spring.xml`.

## 📝 Миграции базы данных

Проект использует Liquibase для управления миграциями БД. Все миграции находятся в `src/main/resources/db/changelog/`.

Миграции применяются автоматически при запуске приложения.

## 🤝 Вклад в проект

1. Fork проекта
2. Создайте feature branch (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в branch (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект создан для личного использования.

## 👤 Автор

Проект создан для java-holic клуба.

## 🔗 Полезные ссылки

- [Telegram Bot API](https://core.telegram.org/bots/api)
- [HH.ru API Documentation](https://github.com/hhru/api)
- [Spring Boot Documentation](https://spring.io/projects/spring-boot)
- [Liquibase Documentation](https://www.liquibase.org/documentation)

