# План доработки проекта: Интеграция с API hh.ru для публикации вакансий

## Цель
Добавить третью функцию бота: автоматическая публикация актуальных вакансий из hh.ru в 8:00 утра за вчерашнюю дату в указанных странах/регионах.

## Анализ текущей архитектуры

### Существующие компоненты:
1. **QuartzMessageBot** - основной класс бота, обрабатывает команды и callback-запросы
2. **ScheduledExecutor** - сервис с scheduled методами для отправки сообщений в 7:00
3. **ActiveChat** - модель для хранения настроек чатов (english, java флаги)
4. **ActiveChatService** - сервис для управления настройками чатов
5. **JavaMessageService / EnglishMessageService** - сервисы для работы с вопросами

### API hh.ru:
- **Эндпоинт**: `GET /vacancies`
- **Ключевые параметры**:
  - `area` - ID региона/страны (можно указать несколько)
  - `date_from` - дата начала поиска (формат: `YYYY-MM-DD`)
  - `date_to` - дата окончания поиска
  - `text` - поиск по тексту (например, "Java")
  - `professional_role` - профессиональная роль
  - `per_page` - количество результатов (макс. 100)
  - `page` - номер страницы

## Этапы реализации

### Этап 1: Подготовка инфраструктуры и зависимостей

#### 1.1. Добавление зависимостей в `build.gradle`
- **HTTP клиент**: Spring Web (уже должен быть через Spring Boot Starter)
- **JSON обработка**: Jackson (уже включен в Spring Boot)
- **Проверка**: убедиться, что `spring-boot-starter-web` присутствует

#### 1.2. Настройка конфигурации
- Добавить в `application.properties`:
  - `hh.api.base-url=https://api.hh.ru`
  - `hh.api.user-agent=QuartzBot/1.0 (idv1llka@gmail.com)` - заголовок HTTP User-Agent для идентификации бота в запросах к API hh.ru (обязательно, API требует этот заголовок)
    - Формат: `НазваниеБота/Версия (email или контакт)`
    - Пример значения: `QuartzBot/1.0 (idv1llka@gmail.com)`
  - `hh.vacancies.search-text=Java` (текст для поиска вакансий)
  - `hh.vacancies.professional-role-id` (ID роли "Программист, разработчик" - обычно "96")
  - `hh.vacancies.default-areas=113,16` (ID регионов по умолчанию: 113 - Россия, 16 - Беларусь)

### Этап 2: Создание моделей и DTO

#### 2.1. DTO для ответа API hh.ru
**Файл**: `src/main/java/org/khodyko/quartzbot/dto/hh/`
- `VacancyResponseDto.java` - основной ответ API с полями:
  - `found` (Integer) - количество найденных вакансий
  - `pages` (Integer) - количество страниц
  - `per_page` (Integer) - элементов на странице
  - `page` (Integer) - текущая страница
  - `items` (List<VacancyItemDto>) - список вакансий
- `VacancyItemDto.java` - элемент вакансии:
  - `id` (String) - ID вакансии
  - `name` (String) - название
  - `alternate_url` (String) - ссылка на вакансию
  - `published_at` (String) - дата публикации
  - `area` (AreaDto) - регион
  - `employer` (EmployerDto) - работодатель
  - `salary` (SalaryDto) - зарплата
  - `snippet` (SnippetDto) - краткое описание (requirement + responsibility)
  - `description` (String) - полное описание вакансии (опционально, может быть null)
- `AreaDto.java` - регион:
  - `id` (String) - ID региона
  - `name` (String) - название региона
- `EmployerDto.java` - работодатель:
  - `id` (String)
  - `name` (String)
  - `alternate_url` (String)
- `SalaryDto.java` - зарплата:
  - `from` (Integer)
  - `to` (Integer)
  - `currency` (String)
  - `gross` (Boolean)
- `SnippetDto.java` - краткое описание:
  - `requirement` (String)
  - `responsibility` (String)

#### 2.2. Обновление модели ActiveChat
**Файл**: `src/main/java/org/khodyko/quartzbot/model/ActiveChat.java`
- Добавить поле `vacancies` (Boolean) - флаг включения вакансий
- Добавить поле `vacancyAreas` (String) - JSON массив ID регионов для поиска вакансий
  - Альтернатива: создать отдельную таблицу `chat_vacancy_areas` с Many-to-Many связью
  - **Рекомендация**: начать с простого варианта - JSON строка, при необходимости рефакторить

#### 2.3. Создание миграции Liquibase
**Файл**: `src/main/resources/db/changelog/changelog_3/vacancies_support.sql`
- Добавить колонку `vacancies` (BOOLEAN DEFAULT FALSE)
- Добавить колонку `vacancy_areas` (TEXT) - JSON массив ID регионов
- Обновить `db.changelog-master.yaml` для включения новой миграции

### Этап 3: Создание сервисов для работы с API hh.ru

#### 3.1. Интерфейс HhApiService
**Файл**: `src/main/java/org/khodyko/quartzbot/service/HhApiService.java`
```java
public interface HhApiService {
    /**
     * Получает список вакансий по заданным параметрам
     * @param areaIds список ID регионов
     * @param dateFrom дата начала поиска (YYYY-MM-DD)
     * @param dateTo дата окончания поиска (YYYY-MM-DD)
     * @param page номер страницы
     * @param perPage количество элементов на странице
     * @return список вакансий
     */
    VacancyResponseDto searchVacancies(List<String> areaIds, String dateFrom, String dateTo, int page, int perPage);
    
    /**
     * Получает справочник регионов
     * @return список регионов
     */
    List<AreaDto> getAreas();
}
```

#### 3.2. Реализация HhApiServiceImpl
**Файл**: `src/main/java/org/khodyko/quartzbot/service/impl/HhApiServiceImpl.java`
- Использовать `RestTemplate` или `WebClient` для HTTP запросов
- Настроить базовый URL из конфигурации
- **Обязательно** добавить заголовок `User-Agent` во все HTTP запросы к API hh.ru (значение из `hh.api.user-agent`)
  - API hh.ru требует этот заголовок, иначе может вернуть ошибку или предложить пройти капчу
  - Пример: `headers.set("User-Agent", "QuartzBot/1.0 (idv1llka@gmail.com)")`
- Реализовать методы поиска вакансий и получения справочника регионов
- Добавить обработку ошибок и логирование

#### 3.3. Сервис для форматирования сообщений о вакансиях
**Файл**: `src/main/java/org/khodyko/quartzbot/service/VacancyMessageFormatter.java`
- Метод для форматирования одной вакансии в читаемый текст для Telegram:
  - `formatVacancy(VacancyItemDto vacancy)` - форматирует одну вакансию для отдельного сообщения
- Метод для получения флага эмодзи по ID региона (см. маппинг ниже)
- Метод для обрезки и очистки описания вакансии:
  - `truncateDescription(String description, int maxLength)` - обрезать до указанной длины (600 символов)
  - `cleanHtmlTags(String text)` - удалить HTML-теги
  - `normalizeWhitespace(String text)` - нормализовать пробелы и переносы строк
- Метод для форматирования итогового сообщения:
  - `formatSummary(int totalCount, String date)` - сообщение с количеством найденных вакансий
- Ограничение длины одного сообщения (Telegram лимит 4096 символов) - каждая вакансия отдельно
- **Формат сообщения (каждая вакансия отдельным сообщением)**:
  ```
  💼 Java Developer
  🇷🇺 Россия | Москва
  🏢 HeadHunter
  💰 150 000 - 250 000 руб.
  
  📝 Разработка backend-сервисов на Java, Spring Boot, PostgreSQL. 
  Работа с микросервисной архитектурой, Docker, Kubernetes. 
  Опыт работы от 3 лет. Удаленная работа возможна.
  
  🔗 https://hh.ru/vacancy/123456789
  ```
- **Особенности формата**:
  - Каждая вакансия отправляется отдельным сообщением
  - В начале сообщения - название вакансии
  - Флаг региона и название региона/города
  - Название компании
  - Зарплата (если указана)
  - Описание (до 600 символов)
  - Ссылка на вакансию
  - После всех вакансий отправляется итоговое сообщение с количеством найденных вакансий
- **Правила форматирования описания**:
  - Использовать `snippet.requirement` + `snippet.responsibility` (объединить через пробел)
  - Обрезать описание до 600 символов (включая пробелы)
  - Если описание обрезано - добавить в конец `...`
  - Удалить HTML-теги из описания (если есть в snippet)
  - Заменить множественные пробелы и переносы строк на один пробел
  - Если оба поля snippet пустые или null - не показывать поле `📝`
  - Если одно из полей пустое - использовать только непустое поле

#### 3.4. Маппинг регионов на флаги эмодзи
**Файл**: `src/main/java/org/khodyko/quartzbot/service/VacancyMessageFormatter.java` или отдельный класс `AreaFlagMapper.java`
- Создать Map<String, String> для маппинга ID региона -> флаг эмодзи
- Маппинг регионов (ID -> флаг):
  - **СНГ и Восточная Европа**:
    - `113` - 🇷🇺 Россия
    - `16` - 🇧🇾 Беларусь
    - `40` - 🇰🇿 Казахстан
    - `5` - 🇺🇦 Украина
    - `9` - 🇦🇿 Азербайджан
    - `11` - 🇦🇲 Армения
    - `28` - 🇬🇪 Грузия
    - `115` - 🇰🇬 Кыргызстан
    - `174` - 🇲🇩 Молдова
    - `172` - 🇹🇯 Таджикистан
    - `173` - 🇹🇲 Туркменистан
    - `99` - 🇺🇿 Узбекистан
  - **Европа**:
    - `104` - 🇵🇱 Польша (если доступно)
    - `100` - 🇩🇪 Германия (если доступно)
    - `97` - 🇬🇧 Великобритания (если доступно)
    - `101` - 🇫🇷 Франция (если доступно)
    - `102` - 🇪🇸 Испания (если доступно)
    - `103` - 🇮🇹 Италия (если доступно)
    - `105` - 🇳🇱 Нидерланды (если доступно)
    - `106` - 🇨🇿 Чехия (если доступно)
  - **Азия**:
    - `159` - 🇨🇳 Китай (если доступно)
    - `160` - 🇯🇵 Япония (если доступно)
    - `161` - 🇰🇷 Южная Корея (если доступно)
    - `162` - 🇮🇳 Индия (если доступно)
    - `163` - 🇸🇬 Сингапур (если доступно)
    - `164` - 🇹🇭 Таиланд (если доступно)
    - `165` - 🇻🇳 Вьетнам (если доступно)
    - `166` - 🇵🇭 Филиппины (если доступно)
  - **Америка**:
    - `84` - 🇺🇸 США (если доступно)
    - `85` - 🇨🇦 Канада (если доступно)
    - `86` - 🇧🇷 Бразилия (если доступно)
    - `87` - 🇲🇽 Мексика (если доступно)
    - `88` - 🇦🇷 Аргентина (если доступно)
- Если флаг не найден - использовать дефолтный эмодзи 📍
- Метод `getFlagByAreaId(String areaId)` - возвращает флаг или дефолтный символ

### Этап 4: Обновление ActiveChatService

#### 4.1. Расширение интерфейса ActiveChatService
**Файл**: `src/main/java/org/khodyko/quartzbot/service/ActiveChatService.java`
- `updateVacanciesByChatId(String chatId, boolean isVacancies)` - включение/выключение вакансий
- `addVacancyAreaByChatId(String chatId, String areaId)` - добавить регион в список
- `removeVacancyAreaByChatId(String chatId, String areaId)` - удалить регион из списка
- `getVacancyAreasByChatId(String chatId)` - получить список регионов для чата (если пусто - вернуть дефолтные)
- `setVacancyAreasByChatId(String chatId, List<String> areaIds)` - установка полного списка регионов (заменяет текущий)
- `getActiveVacancyChats()` - получение списка чатов с включенными вакансиями

#### 4.2. Реализация в ActiveChatServiceImpl
**Файл**: `src/main/java/org/khodyko/quartzbot/service/impl/ActiveChatServiceImpl.java`
- Реализовать новые методы
- `getVacancyAreasByChatId()` - если `vacancyAreas` пусто или null, вернуть дефолтные из конфигурации
- `getVacancyAreasWithNames(String chatId)` - получить список регионов с их названиями и флагами для отображения
- `addVacancyAreaByChatId()` - добавить регион, если его еще нет в списке (избегать дубликатов)
- `addVacancyAreaByName(String chatId, String areaName)` - найти регион по названию и добавить
  - Использовать `HhApiService.getAreas()` для поиска
  - Поиск case-insensitive, частичное совпадение
  - Если найдено несколько - вернуть список для выбора
  - Если не найдено - выбросить исключение `AreaNotFoundException`
- `removeVacancyAreaByChatId()` - удалить регион из списка
- `removeVacancyAreaByName(String chatId, String areaName)` - найти регион по названию и удалить
- При первом включении вакансий - инициализировать дефолтными регионами, если список пуст
- Добавить валидацию areaIds через `HhApiService.getAreas()`
- Обработка JSON для vacancyAreas (использовать Jackson ObjectMapper)

### Этап 5: Добавление scheduled задачи для отправки вакансий

#### 5.1. Обновление ScheduledExecutor
**Файл**: `src/main/java/org/khodyko/quartzbot/bots/quartz/ScheduledExecutor.java`
- Добавить метод `sendVacancyMessages()` с аннотацией `@Scheduled(cron = "0 0 8 * * ?")` (8:00 утра)
- Логика метода:
  1. Получить список активных чатов с включенными вакансиями
  2. Для каждого чата:
     - Получить список регионов через `getVacancyAreasByChatId()` (если пусто - используются дефолтные из конфигурации)
     - Получить вчерашнюю дату в формате `YYYY-MM-DD` (использовать `LocalDate.now().minusDays(1)`)
     - Вызвать `HhApiService.searchVacancies()` с параметрами:
       - `areaIds` - регионы из настроек чата или дефолтные
       - `dateFrom` - вчерашняя дата
       - `dateTo` - вчерашняя дата
       - `text` - "Java" (из конфигурации)
       - `perPage` - 100 (максимум для получения всех вакансий)
     - Если вакансий больше 100 - обработать все страницы (использовать цикл по `pages`)
     - Для каждой найденной вакансии:
       - Отформатировать вакансию через `VacancyMessageFormatter.formatVacancy()`
       - Отправить отдельное сообщение в чат
     - После отправки всех вакансий отправить итоговое сообщение с количеством найденных вакансий
     - Добавить небольшую задержку между сообщениями (например, 100-200 мс) для избежания rate limiting Telegram

### Этап 6: Обновление UI бота (QuartzMessageBot)

#### 6.1. Добавление кнопок управления вакансиями
**Файл**: `src/main/java/org/khodyko/quartzbot/bots/quartz/QuartzMessageBot.java`
- Добавить константы для callback:
  - `VACANCIES_ON_BTN = "vacanciesOn"`
  - `VACANCIES_OFF_BTN = "vacanciesOff"`
  - `VACANCIES_SET_AREAS_BTN = "vacanciesSetAreas"`
  - `VACANCIES_SHOW_AREAS_BTN = "vacanciesShowAreas"` - показать текущие регионы
  - `VACANCIES_REMOVE_AREA_BTN = "vacanciesRemoveArea"` - удалить регион
- Обновить `handleButtonChatSettings()` - добавить кнопки для управления вакансиями
- Добавить обработку callback в `handleCallbackQuery()`:
  - `VACANCIES_ON_BTN` - включить вакансии, показать текущие регионы с сообщением:
    ```
    ✅ Вакансии включены!
    
    📋 Текущие регионы для поиска:
    🇷🇺 Россия (113)
    🇧🇾 Беларусь (16)
    
    Используйте команды для управления регионами.
    ```
  - `VACANCIES_OFF_BTN` - выключить вакансии
  - `VACANCIES_SET_AREAS_BTN` - показать меню управления регионами с кнопками и текущим списком регионов
  - `VACANCIES_SHOW_AREAS_BTN` - показать текущий список регионов с названиями и флагами
  - `VACANCIES_REMOVE_AREA_BTN` - показать список текущих регионов с кнопками удаления
- Добавить обработку текстовых команд в `handleIncomingMessage()`:
  - `vacancies_area_add <название>` - добавить регион по названию
  - `vacancies_area_remove <название>` - удалить регион
  - `vacancies_area_list` - показать текущие регионы
- При обработке команд использовать try-catch для обработки `AreaNotFoundException` и других ошибок
- Показывать понятные сообщения об ошибках пользователю

#### 6.2. Команды для управления регионами

**Текстовые команды:**
- `vacancies_area_add <название>` - добавить регион по названию (например: `vacancies_area_add Россия` или `vacancies_area_add Москва`)
  - Бот ищет регион в справочнике через `HhApiService.getAreas()` по названию (case-insensitive, частичное совпадение)
  - Если найдено несколько регионов - показать список с названиями и попросить уточнить запрос
  - Если регион не найден - сообщение: "❌ Регион '[название]' не найден. Проверьте правильность написания."
  - Если регион уже добавлен - сообщение: "⚠️ Регион '[название]' уже добавлен в список."
  - При успешном добавлении - показать обновленный список регионов
- `vacancies_area_remove <название>` - удалить регион по названию (например: `vacancies_area_remove Россия`)
  - Поиск по названию в текущем списке регионов
  - Если регион не найден в списке - сообщение: "❌ Регион '[название]' не найден в вашем списке."
- `vacancies_area_list` - показать текущий список регионов с их названиями и флагами:
  ```
  📋 Ваши регионы для поиска вакансий:
  
  🇷🇺 Россия (113)
  🇧🇾 Беларусь (16)
  🇰🇿 Казахстан (40)
  
  Всего: 3 региона
  ```

**Интерактивное меню (через кнопки):**
- При нажатии `VACANCIES_SET_AREAS_BTN` показать меню:
  - Кнопка "📋 Показать текущие регионы" - вызывает `vacancies_area_list`
  - Кнопка "➖ Удалить регион" - показать список текущих регионов с кнопками удаления
  - Информация: "Для добавления региона используйте команду: vacancies_area_add <название>"

**Валидация и обработка ошибок:**
- Проверять существование регионов через `HhApiService.getAreas()`
- При добавлении: проверять, что регион еще не добавлен (избегать дубликатов)
- При удалении: проверять, что регион существует в списке
- При поиске: если ничего не найдено - сообщение: "❌ Регионы по запросу '[название]' не найдены."
- При отображении списка: если список пуст - показать: "📋 Список регионов пуст. Используются дефолтные регионы: 🇷🇺 Россия (113), 🇧🇾 Беларусь (16)"

### Этап 7: Обработка ошибок и логирование

#### 7.1. Обработка ошибок API
- Обработка HTTP ошибок (429 - rate limit, 500 - server error)
- Логирование всех ошибок через SLF4J
- Отправка ошибок через `SendMeService`

#### 7.2. Валидация данных
- Валидация areaIds перед сохранением
- Проверка формата дат
- Обработка пустых результатов поиска
- Создать исключение `AreaNotFoundException` для случаев, когда регион не найден
- Валидация названий регионов при поиске (проверка на пустоту, минимальная длина)

### Этап 8: Оптимизация и улучшения (опционально)

#### 8.1. Кэширование справочника регионов
- Кэшировать справочник регионов (обновлять раз в день)
- Использовать Spring Cache

#### 8.2. Дедупликация вакансий
- Сохранять ID отправленных вакансий в БД
- Не отправлять повторно уже отправленные вакансии

#### 8.3. Настройка фильтров вакансий
- Добавить возможность настройки текста поиска
- Добавить фильтр по зарплате
- Добавить фильтр по типу занятости

#### 8.4. Статистика
- Сохранять статистику отправленных вакансий
- Отчет о количестве найденных/отправленных вакансий

## Порядок реализации (приоритет)

1. **Этап 1** - Подготовка инфраструктуры
2. **Этап 2** - Создание моделей и DTO
3. **Этап 3** - Создание сервисов для работы с API
4. **Этап 4** - Обновление ActiveChatService
5. **Этап 5** - Добавление scheduled задачи
6. **Этап 6** - Обновление UI бота
7. **Этап 7** - Обработка ошибок
8. **Этап 8** - Оптимизация (по необходимости)

## Важные замечания

1. **Rate Limiting**: Так как запрос к API выполняется только один раз в день, rate limiting не критичен. Однако рекомендуется обрабатывать ошибку 429 (Too Many Requests) на всякий случай.

2. **Формат дат**: Использовать `LocalDate` и `DateTimeFormatter` для работы с датами в формате ISO 8601

3. **Длина сообщений**: 
   - Telegram ограничивает длину сообщения 4096 символами
   - Каждая вакансия отправляется отдельным сообщением
   - Описание вакансии обрезается до 600 символов
   - Если одна вакансия превышает лимит - обрезать описание еще больше или убрать его
   - После всех вакансий отправляется итоговое сообщение с количеством найденных вакансий

4. **Регионы**: 
   - **Дефолтные регионы**: 113 (Россия 🇷🇺), 16 (Беларусь 🇧🇾)
   - Пользователи могут добавлять дополнительные регионы через команды
   - Если список регионов пуст - используются дефолтные
   - **Популярные ID регионов с флагами**:
     - **СНГ**: 113 🇷🇺, 16 🇧🇾, 40 🇰🇿, 5 🇺🇦, 9 🇦🇿, 11 🇦🇲, 28 🇬🇪, 115 🇰🇬, 174 🇲🇩, 172 🇹🇯, 173 🇹🇲, 99 🇺🇿
     - **Города России**: 1 (Москва), 2 (Санкт-Петербург)
     - **Европа**: 104 🇵🇱, 100 🇩🇪, 97 🇬🇧, 101 🇫🇷, 102 🇪🇸, 103 🇮🇹, 105 🇳🇱, 106 🇨🇿 (если доступно в API)
     - **Азия**: 159 🇨🇳, 160 🇯🇵, 161 🇰🇷, 162 🇮🇳, 163 🇸🇬, 164 🇹🇭, 165 🇻🇳, 166 🇵🇭 (если доступно в API)
     - **Америка**: 84 🇺🇸, 85 🇨🇦, 86 🇧🇷, 87 🇲🇽, 88 🇦🇷 (если доступно в API)
   - Справочник можно получить через `/areas` API
   - **Примечание**: ID регионов для Европы, Азии и Америки могут отличаться или отсутствовать в hh.ru API. Необходимо проверить актуальные ID через `/areas` API перед использованием

5. **Профессиональная роль**: Для Java-разработчиков обычно используется `professional_role=96` ("Программист, разработчик")

## Ссылки на документацию

- API hh.ru: https://api.hh.ru/openapi/redoc
- Эндпоинт поиска вакансий: `GET /vacancies`
- Справочник регионов: `GET /areas`

